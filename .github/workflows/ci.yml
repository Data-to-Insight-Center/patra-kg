name: CI

on: [push]

jobs:
  start:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Run tests
        run: |
          pip install -r tests/requirements.txt
          python3 -m unittest tests/test_patra_rest_services.py